import type { DefaultOptions } from "@/types/message-to-hast.ts";

const DEFAULT_OPTIONS: DefaultOptions = {
  classPrefix: "tg-",
  preserveEntityData: false,
  withClass: true,
};

export { DEFAULT_OPTIONS };
